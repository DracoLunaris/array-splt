




## Development server

Run `ng serve --open` for a dev server. THis will navigate to `http://localhost:4200/` on your web browser. 
## Running unit tests

Run `ng test` to execute the unit tests via [Karma](https://karma-runner.github.io).

